{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities available to the main CubbyDB window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
